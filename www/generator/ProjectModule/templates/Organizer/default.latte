{block content}

    {snippet obsah}

        <a class="ajax button newItem" n:href="editOrganizer!, id=>null">New Organizer</a>

<fieldset class="fieldset">
    <h1>List of Organizer</h1>


    <table class="tableOne">
        <thead>
        <tr>
            
<th>Actions</th>
<th>userId</th>
<th>eventId</th>
<th>role</th>
<th>createDate</th>
        </tr>
        </thead>
        <tbody>
        {foreach $list as $item}
            <tr>
                <td><a class="ajax" n:href="editOrganizer!, id=>$item->idOrganizer"><img
                                src="{$basePath}/images/icon/accessories-text-editor.png"/></a>
                    <a class="delete" n:href="deleteOrganizer!, id=>$item->idOrganizer"><img
                                src="{$basePath}/images/icon/edit-clear.png"/></a>
                </td>
                

<td>{$item->userId}</td>
<td>{$item->eventId}</td>
<td>{$item->role}</td>
<td>{$item->createDate}</td>
            </tr>
        {/foreach}
        </tbody>
    </table>
    <br/>

</fieldset>
        <script>
            $(function(){
                $(".delete").click(function(){
                    if (!confirm('Opravdu si přejete položku smazat?')) return false;
                });
            });
        </script>

{/snippet}

{snippet editOrganizer}
    {ifset $editOrganizer}
        <div id="editOrganizer" class="modalDisable">
            {form organizerForm}

                <!-- Jednoduché vykreslení chyb -->
                <ul class="errors" n:if="$form->hasErrors()">
                    <li n:foreach="$form->errors as $error">{$error}</li>
                </ul>

                <fieldset class="edit" style="width:100%; text-align:left; padding:0px 10px; margin:0px;">

                    <table class="tableTwo">
                        

<tr><td class="required">{label userId /}{input userId}</td></tr>
<tr><td class="required">{label eventId /}{input eventId}</td></tr>
<tr><td class="required">{label role /}{input role}</td></tr>
<tr><td class="required">{label createDate /}{input createDate}</td></tr>
                    </table>
                </fieldset>

                <fieldset class="edit" style="width:100%; text-align:right; padding:0px 10px; margin-top:10px;">
                    {input send}
                </fieldset>


            {/form}

            {*{control organizerForm}*}


        </div>
        <script>
            $(function () {
                var last;
                var dialogOptions = {
                    "title": "Detail položky",
                    "width": 500,
                    "height": 450,
                    "minWidth": 500,
                    "minHeight": 450,
                    "modal": false,
                    "resizable": true,
                    "draggable": true,
                    "cancel": false,
                    "position": {"my": "center top", "at": "center bottom", "of": "button"},
                    "close": function () {
                        if (last != this) {
                            $(last).dialog("destroy").remove();
                        }
                    }
                };
                var dialogExtendOptions = {
                    "closable": true,
                    "maximizable": false,
                    "minimizable": false,
                    "minimizeLocation": "left",
                    "collapsable": false,
                    "dblclick": "maximize",
                    "titlebar": false
                };
                last = $("#editOrganizer").dialog(dialogOptions).dialogExtend(dialogExtendOptions);
                maxwidth = $('html').css('max-width');

                if (maxwidth == '1200px') {
                    last.dialogExtend('maximize');
                }

                $("select").chosen();
                $('span.ui-dialog-title').each(function () {
                    var $p = $(this);
                    $p.html($p.html().replace(/^(\w+)/, '<strong>$1</strong>'));
                });



            });
        </script>
    {/ifset}
{/snippet}