{block content}

    {snippet obsah}

        <a class="ajax button newItem" n:href="editRentalItem!, id=>null">New RentalItem</a>

<fieldset class="fieldset">
    <h1>List of RentalItem</h1>


    <table class="tableOne">
        <thead>
        <tr>
            
<th>Actions</th>
<th>stockItemId</th>
<th>createDate</th>
<th>rentalId</th>
        </tr>
        </thead>
        <tbody>
        {foreach $list as $item}
            <tr>
                <td><a class="ajax" n:href="editRentalItem!, id=>$item->rentalItemId"><img
                                src="{$basePath}/images/icon/accessories-text-editor.png"/></a>
                    <a class="delete" n:href="deleteRentalItem!, id=>$item->rentalItemId"><img
                                src="{$basePath}/images/icon/edit-clear.png"/></a>
                </td>
                

<td>{$item->stockItemId}</td>
<td>{$item->createDate}</td>
<td>{$item->rentalId}</td>
            </tr>
        {/foreach}
        </tbody>
    </table>
    <br/>

</fieldset>
        <script>
            $(function(){
                $(".delete").click(function(){
                    if (!confirm('Opravdu si přejete položku smazat?')) return false;
                });
            });
        </script>

{/snippet}

{snippet editRentalItem}
    {ifset $editRentalItem}
        <div id="editRentalItem" class="modalDisable">
            {form rentalItemForm}

                <!-- Jednoduché vykreslení chyb -->
                <ul class="errors" n:if="$form->hasErrors()">
                    <li n:foreach="$form->errors as $error">{$error}</li>
                </ul>

                <fieldset class="edit" style="width:100%; text-align:left; padding:0px 10px; margin:0px;">

                    <table class="tableTwo">
                        

<tr><td class="required">{label stockItemId /}{input stockItemId}</td></tr>
<tr><td class="required">{label createDate /}{input createDate}</td></tr>
<tr><td class="required">{label rentalId /}{input rentalId}</td></tr>
                    </table>
                </fieldset>

                <fieldset class="edit" style="width:100%; text-align:right; padding:0px 10px; margin-top:10px;">
                    {input send}
                </fieldset>


            {/form}

            {*{control rentalItemForm}*}


        </div>
        <script>
            $(function () {
                var last;
                var dialogOptions = {
                    "title": "Detail položky",
                    "width": 500,
                    "height": 450,
                    "minWidth": 500,
                    "minHeight": 450,
                    "modal": false,
                    "resizable": true,
                    "draggable": true,
                    "cancel": false,
                    "position": {"my": "center top", "at": "center bottom", "of": "button"},
                    "close": function () {
                        if (last != this) {
                            $(last).dialog("destroy").remove();
                        }
                    }
                };
                var dialogExtendOptions = {
                    "closable": true,
                    "maximizable": false,
                    "minimizable": false,
                    "minimizeLocation": "left",
                    "collapsable": false,
                    "dblclick": "maximize",
                    "titlebar": false
                };
                last = $("#editRentalItem").dialog(dialogOptions).dialogExtend(dialogExtendOptions);
                maxwidth = $('html').css('max-width');

                if (maxwidth == '1200px') {
                    last.dialogExtend('maximize');
                }

                $("select").chosen();
                $('span.ui-dialog-title').each(function () {
                    var $p = $(this);
                    $p.html($p.html().replace(/^(\w+)/, '<strong>$1</strong>'));
                });



            });
        </script>
    {/ifset}
{/snippet}